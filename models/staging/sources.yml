version: 2

sources:
  - name: raw
    database: ATMOSYNC_DB
    schema: RAW
    tables:
      - name: RAW_KAFKA_TELEMETRY
        description: Raw JSON telemetry log records streamed from the IoT container sensors.
